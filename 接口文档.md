# 临床研究核心词汇小程序 接口文档

**文档版本**：v1.0
**最后更新**：2026-04-29
**业务描述**：本接口文档为临床研究核心词汇学习小程序的后端接口规范，涵盖用户登录、VIP支付、首页数据、单词学习、复习巩固等全业务流程接口。

## 通用说明

1.  **接口基础地址**：`localhost:8080`
2.  **数据格式**：所有请求与响应均采用 JSON 格式
3.  **通用响应结构**：
    ```json
    {
      "code": 200,
      "msg": "操作成功",
      "data": {}
    }
    ```
    | 字段 | 类型              | 说明                              |
    | ---- | ----------------- | --------------------------------- |
    | code | int               | 响应状态码，200为成功，其他为失败 |
    | msg  | string            | 响应信息描述                      |
    | data | object/array/null | 响应业务数据，无数据时返回null    |
4.  **认证规则**：除登录、支付相关接口外，其余所有接口均需从请求头`header`中携带`token`，token值为用户`openid`，后端通过token获取用户身份信息。

---

## 一、登录模块

### 1.1 小程序登录接口

**接口名称**：小程序登录
**接口地址**：`/wechatLogin`
**请求方式**：GET
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| code | string | 是 | 微信小程序登录授权获取的code |

**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| openid | string | 微信用户唯一标识openid |
| vip | int | 用户VIP状态，1=是VIP用户，0=非VIP用户 |

---

## 二、VIP支付模块

### 2.1 支付请求接口

**接口名称**：支付请求接口
**接口地址**：`/pay/createOrder`
**请求方式**：POST
**接口描述**：前端小程序发起支付请求，后端完成微信统一下单、签名加密等流程，返回前端支付所需参数
**业务流程**：

1.  前端小程序发起支付请求 → 后端
2.  后端调用微信统一下单接口预下单
3.  微信返回预支付信息（prepay_id）
4.  后端签名加密返回前端支付参数
5.  小程序调用微信支付拉起收银台
6.  用户支付完成 → 微信异步回调后端支付结果通知
7.  后端更新订单状态，设置用户为VIP
8.  前端轮询 / 推送查询支付结果

**接口入参**：无特殊入参，后端通过header的token获取用户openid
**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| prepay_id | string | 微信统一下单返回的预支付会话标识 |
| paySign | string | 后端签名后的支付签名 |
| nonceStr | string | 随机字符串，用于签名 |
| timeStamp | string | 时间戳，用于签名 |
| signType | string | 签名类型，固定为MD5 |

### 2.2 微信支付回调接口

**接口名称**：微信支付回调
**接口地址**：`/pay/wechatCallback`
**请求方式**：POST
**接口描述**：微信支付成功后，微信平台异步调用该接口，后端接收支付结果，更新订单状态并将用户设置为VIP用户
**接口入参**：微信平台推送的支付结果XML数据
**接口返参**：微信平台要求的XML格式响应，告知微信回调结果已处理

### 2.3 查询支付结果接口

**接口名称**：查询微信支付回调结果
**接口地址**：`/pay/queryPayResult`
**请求方式**：GET
**接口入参**：无特殊入参，后端通过header的token获取用户openid
**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| vipSetSuccess | boolean | VIP状态是否设置成功，true=成功，false=失败 |
| orderStatus | string | 订单状态，SUCCESS=支付成功，PAYING=支付中，FAIL=支付失败 |

---

## 三、首页模块

### 3.1 首页信息查询接口

**接口名称**：首页信息查询
**接口地址**：`/word/index/getIndexInfo`
**请求方式**：GET
**接口入参**：无特殊入参，后端通过header的token获取用户openid
**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| accuracy | string | 答题正确率，计算公式：已答正确单词数/已答单词数*100% |
| streak | int | 连续坚持学习天数 |
| allDays | int | 理论学习总天数，固定为200天 |
| progress | string | 学习进度，计算公式：已答单词数/单词总数*100% |
| nextWordId | int | 下一个待背诵单词id，全部背诵完成后返回null |
| allWordsNum | int | 单词库总单词量 |
| recitedWordsNum | int | 用户已背诵单词数量 |
| toReviewWordsNum | int | 用户待复习单词数量 |
| collectWordsNum | int | 用户收藏单词数量 |

**响应示例**：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "recitedWordsNum": 3,
    "allWordsNum": 1020,
    "accuracy": "66.67%",
    "streak": 3,
    "allDays": 200,
    "progress": "0.20%",
    "nextWordId": 4,
    "toReviewWordsNum": 2,
    "collectWordsNum": 0
  }
}
```

---

## 四、单词背诵页模块

### 4.1 根据单词id查询英文题目详情接口

**接口名称**：根据单词id查询英文题目详情
**接口地址**：`/word/recite/getEnglishQuestionDetailByWordId`
**请求方式**：GET
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| wordId | int | 是 | 单词id |

**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| questionContent | string | 英文题目内容 |
| questionAnswerList | array | 题目选项列表 |
| questionAnswerList[].wordId | int | 单词id |
| questionAnswerList[].answer | string | 选项完整答案内容 |
| questionAnswerList[].simpleAnswer | string | 选项简要答案 |
| questionAnswerList[].annotation | string | 选项答案注释解析 |
| questionAnswerList[].correctAnswer | boolean | 是否为正确答案，true=正确，false=错误 |

**响应示例**：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "questionContent": "The clinical trial protocol specified that the __________ of KAT6A/B overexpression in tumor tissues would exclude patients from enrollment, as the drug’s mechanism relies on targeting abnormally activated KAT6.",
    "questionAnswerList": [
      {
        "wordId": 7,
        "answer": "absence",
        "simpleAnswer": "absence",
        "annotation": "句中明确药物作用机制依赖靶向异常激活的 KAT6，因此肿瘤组织中 KAT6A/B 过表达的 “缺失”（absence）会导致患者不符合入组条件，这符合靶向药物临床试验中生物标志物筛选的核心逻辑。",
        "correctAnswer": true
      },
      {
        "wordId": 7,
        "answer": "overexpression",
        "simpleAnswer": "overexpression（过表达）",
        "annotation": "是该临床试验入组的关键生物标志物，若患者具备该特征应被纳入研究，而非排除。",
        "correctAnswer": false
      },
      {
        "wordId": 7,
        "answer": "presence",
        "simpleAnswer": "presence（存在）",
        "annotation": "若肿瘤组织中存在 KAT6A/B 过表达，患者应符合入组标准，与 “排除患者” 的语境矛盾。",
        "correctAnswer": false
      },
      {
        "wordId": 7,
        "answer": "abundance",
        "simpleAnswer": "abundance（大量存在）",
        "annotation": "指过表达的程度较高，这正是该临床试验希望筛选的入组人群，而非排除。",
        "correctAnswer": false
      }
    ]
  }
}
```

### 4.2 单词背诵答题结果提交接口

**接口名称**：单词背诵
**接口地址**：`/word/recite/reciteWord`
**请求方式**：POST
**接口描述**：用户提交单词背诵答题结果，后端记录答题情况，并判断当天学习坚持天数是否+1
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| wordId | int | 是 | 单词id |
| answerResult | boolean | 是 | 英文题目答题结果，true=回答正确，false=回答错误 |

**请求示例**：

```json
{
  "wordId": 5,
  "answerResult": false
}
```

**接口返参**：无业务数据，返回通用成功响应

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

---

## 五、单词详情页模块

### 5.1 根据单词id查询单词详情接口

**接口名称**：根据单词id查询单词详情
**接口地址**：`/word/word/getWordDetailByWordId`
**请求方式**：GET
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| wordId | int | 是 | 单词id |
| wordType | string | 是 | 单词场景类型，recite=背诵时，review=复习时 |

**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| id | int | 单词id |
| word | string | 单词英文内容 |
| phoneticSymbol | string | 单词音标 |
| definition | string | 单词中文释义 |
| level | string | 单词难度等级，如CET4、CET6、考研、TOEFL等 |
| exampleSentence | string | 临床研究行业例句 |
| collectResult | boolean | 用户是否收藏该单词，true=已收藏，false=未收藏 |
| nextWordId | int | 下一个背诵/复习单词id，全部完成后返回null |

**响应示例**：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "id": 7,
    "word": "absence",
    "phoneticSymbol": "/ˈæbsəns/",
    "definition": "n.缺席，缺勤，不在；缺乏，没有；缺席期间，休假期间；不注意",
    "level": "CET4 | CET6 | 考研 | TOEFL | SAT | 商务英语",
    "exampleSentence": "As another example, absence of affirmative information on a condition or event does not necessarily mean the condition or event is not present.\n另一个示例为，缺乏关于病症或事件的确证信息并不一定意味着该症状或事件不存在。",
    "collectResult": false,
    "nextWordId": 6
  }
}
```

---

## 六、复习详情页模块

### 6.1 根据单词id查询中文题目详情接口

**接口名称**：根据单词id查询中文题目详情
**接口地址**：`/word/review/getChineseQuestionDetailByWordId`
**请求方式**：GET
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| wordId | int | 是 | 单词id |

**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| currentReviewIndex | int | 当前待复习单词序号 |
| totalReviewNum | int | 待复习单词总数 |
| questionContent | string | 中文题目内容 |
| questionAnswerList | array | 题目选项列表，结构同英文题目接口 |

**响应示例**：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "currentReviewIndex": 1,
    "totalReviewNum": 10,
    "questionContent": "在临床试验的疗效评价与安全性监测中，“absence” 最核心的中文释义是？",
    "questionAnswerList": [
      {
        "wordId": 7,
        "answer": " 缺席；未参与（如：因受试者多次缺席随访，无法完成疗效评估）",
        "simpleAnswer": "缺席；未参与",
        "annotation": "符合临床试验中对受试者随访依从性的描述，是absence在临床研究场景的核心释义之一。",
        "correctAnswer": true
      },
      {
        "wordId": 7,
        "answer": "存在；出现（如：患者治疗后出现了预期的疗效反应）",
        "simpleAnswer": "存在；出现",
        "annotation": "与absence的核心语义完全相反，属于干扰选项。",
        "correctAnswer": false
      }
    ]
  }
}
```

### 6.2 单词复习答题结果提交接口

**接口名称**：单词复习答题提交
**接口地址**：`/word/review/reviewWord`
**请求方式**：POST
**接口描述**：用户提交单词复习答题结果，后端记录答题情况，更新单词复习状态
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| wordId | int | 是 | 单词id |
| answerResult | boolean | 是 | 中文题目答题结果，true=回答正确，false=回答错误 |

**接口返参**：无业务数据，返回通用成功响应

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

---

## 七、收藏模块

### 7.1 单词收藏/取消收藏接口

**接口名称**：单词收藏状态切换
**接口地址**：`/word/collect/switchCollect`
**请求方式**：POST
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| wordId | int | 是 | 单词id |
| collectStatus | boolean | 是 | 目标收藏状态，true=收藏，false=取消收藏 |

**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| collectResult | boolean | 最终收藏状态，与入参collectStatus一致 |

### 7.2 收藏单词列表查询接口

**接口名称**：收藏单词列表查询
**接口地址**：`/word/collect/getCollectList`
**请求方式**：GET
**接口入参**：
| 参数名 | 类型 | 是否必传 | 说明 |
|--------|------|----------|------|
| pageNum | int | 否 | 页码，默认1 |
| pageSize | int | 否 | 每页条数，默认20 |

**接口返参**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| total | int | 收藏单词总数 |
| list | array | 收藏单词列表，每个元素包含单词id、单词内容、释义、音标等基础信息 |
| pageNum | int | 当前页码 |
| pageSize | int | 每页条数 |
| totalPage | int | 总页数 |

---

## 错误码说明

| 错误码 | 说明             | 解决方案                                                   |
| ------ | ---------------- | ---------------------------------------------------------- |
| 400    | 请求参数错误     | 检查请求参数是否符合接口规范，必传参数是否缺失             |
| 401    | 未授权/Token无效 | 检查请求头是否携带正确的token，token是否过期               |
| 403    | 权限不足         | 检查用户是否有访问该接口的权限，如VIP接口需确认用户VIP状态 |
| 404    | 接口地址不存在   | 检查接口地址是否正确，是否符合文档规范                     |
| 500    | 服务器内部错误   | 联系后端开发人员排查服务器问题                             |
| 1001   | 微信登录失败     | 检查微信code是否有效，是否已被使用                         |
| 1002   | 支付下单失败     | 检查微信支付配置是否正确，用户openid是否有效               |
| 1003   | 单词id不存在     | 检查传入的wordId是否在单词库中存在                         |
